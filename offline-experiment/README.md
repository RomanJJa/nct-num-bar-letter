

# Experiment for running offline

In order to run the experiment offline, I have written this version here.
Store the contents in the same same directory to allow the browser to access these files.
The browser does not have permissions to open files outside of the HTML document's own directory
when opening it in the `file://` protocol.
You can double-click on the file `main-offline.html` or right click and open it in any browser.
